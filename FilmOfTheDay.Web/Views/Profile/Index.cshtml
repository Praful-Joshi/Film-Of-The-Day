@model FilmOfTheDay.Web.Models.Profile.ProfileViewModel
@using System.Security.Claims
@using FilmOfTheDay.Web.Services.Interfaces;

@{
    ViewData["Title"] = "Profile";
    var loggedInUserId = User.FindFirstValue(ClaimTypes.NameIdentifier);
    var isOwner = loggedInUserId == Model.UserID.ToString();
}

<div class="max-w-5xl mx-auto pt-4">

    <!-- Profile Header -->
    <div class="flex flex-row items-center gap-6 px-4 flex-wrap">
        <img src="~/images/profile-placeholder.png" 
             class="rounded-full w-20 h-20 object-cover" 
             alt="Profile Picture" />

        <div class="flex-1">
            <div class="flex items-center gap-3">
                <h2 class="text-2xl font-bold text-gray-300">@Model.UserName</h2>

                @* Follow button visible only when viewing someone else's profile *@
                @if (!isOwner)
                {
                    <form asp-controller="Connection"
                        asp-action="SendRequest"
                        method="post"
                        class="inline text-sm follow-form"
                        asp-antiforgery="true">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="receiverId" value="@Model.UserID" />

                        @{
                                var buttonText = "Add Friend";
                                var buttonClass = "bg-blue-500 hover:bg-blue-600";
                                var disabled = "";
                                var isAcceptAction = false; // whether button should call AcceptRequest

                                switch (Model.friendshipState)
                                {
                                    case FriendshipState.RequestSent:
                                        buttonText = "Requested";
                                        disabled = "disabled";
                                        break;
                                    case FriendshipState.Friends:
                                        buttonText = "Friends";
                                        disabled = "disabled";
                                        break;
                                    case FriendshipState.RequestReceived:
                                        buttonText = "Accept Request";
                                        isAcceptAction = true;
                                        break;
                                }
                        }

                        @if (isAcceptAction)
                        {
                            <button type="button"
                                    id="followBtn"
                                    data-receiver="@Model.UserID"
                                    class="px-3 mb-1 py-1.5 font-semibold text-white rounded-full shadow-sm active:scale-95 transition-transform duration-150 @buttonClass">
                                @buttonText
                            </button>
                        }
                        else
                        {
                            <button type="submit"
                                    id="followBtn"
                                    class="px-3 mb-1 py-1.5 font-semibold text-white rounded-full shadow-sm active:scale-95 transition-transform duration-150 @buttonClass"
                                    @disabled>
                                @buttonText
                            </button>
                        }
                    </form>
                }

            </div>

            <p class="text-gray-400">@Model.Email</p>

            <div class="flex gap-6 mt-3 text-gray-300">
                <div><strong>@Model.PostCount</strong> Posts</div>
                <div><strong>@Model.FriendsCount</strong> Friends</div>
            </div>
        </div>
    </div>

    <!-- Tabs -->
    <div class="flex justify-center border-b border-gray-700 pt-4">
        <button class="tab-btn text-gray-300 font-medium py-2 px-4"
                onclick="showTab('posts')">Posts</button>

        @if (isOwner)
        {
            <button class="tab-btn text-gray-500 font-medium py-2 px-4"
                    onclick="showTab('saved')">Saved</button>
        }
    </div>


    <!-- Posts Tab -->
    <div id="posts" class="tab-content grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4">
        @if (Model.Posts.Any())
        {
            foreach (var post in Model.Posts)
            {
                <div class="cursor-pointer overflow-hidden border aspect-square"
                    onclick="window.location.href='/post/viewpost?id=@post.Id'">
                    <img src="@post.ImageUrl"
                        class="w-full h-full object-cover transition-transform duration-300 hover:scale-105" />
                </div>

            }
        }
        else
        {
            <p class="col-span-full text-center text-gray-500 mt-4">No posts yet.</p>
        }
    </div>

    <!-- Saved Tab -->
    @if (isOwner)
    {
        <div id="saved" class="tab-content hidden text-center text-gray-500 mt-4">
            Saved posts will appear here.
        </div>
    }
</div>

@section Scripts {
    <script src="~/js/pages/profile.js" defer></script>
}

